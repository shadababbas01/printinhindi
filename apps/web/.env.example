# Frontend env — SAFE for the browser bundle. Never put secret/service-role
# keys here; Vite inlines every VITE_* value into the built JS.

VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
VITE_API_BASE_URL=http://localhost:8787
VITE_CASHFREE_MODE=sandbox
VITE_UPI_ID=
VITE_UPI_PAYEE_NAME=
